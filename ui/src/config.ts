export const config = {
    /**
     * Controls whether we render a prominent bug linking to S2. Currently this is disabled
     * because the full CORD-19 set hasn't yet been ingested.
     */
    ENABLE_S2_LINKS: false
};
